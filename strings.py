# Strings
print("\nStrings in python are rerepesented as immutable arrays of unicode characters; there are no char types in python")

# Defining a string
String = "I am a string"
print("\nDefining a string")
print(String)

# Accessing a string
print("\n Print first character")
print(String[0])
print("\n Print last character")
print(String[-1])

# Since strings are immutable, operations that change a string will create a copy
print("\nSince strings are immutable, operations that change a string will create a copy")
